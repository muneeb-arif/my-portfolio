/**
 * Meta tags are emitted server-side via Next.js generateMetadata (see src/app/page.tsx).
 * This component is kept as a no-op so any stray imports do not mutate document.head.
 */
export default function DynamicHead() {
  return null;
}
