/**
 * @deprecated Use apiService directly. Supabase hybrid paths were removed (Vercel Postgres + Blob).
 */
import { apiService } from './apiService';

export const hybridService = apiService;
export default apiService;
