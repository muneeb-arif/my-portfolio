/** Logical storage buckets (Vercel Blob path prefixes via API). */
export const BUCKETS = {
  IMAGES: 'images',
  AVATARS: 'avatars',
  DOCUMENTS: 'documents',
  DOMAINS: 'domains',
  UPDATES: 'updates',
};

export default BUCKETS;
